import React from 'react';

export default function Vj59child({name, age, children}) {

        return(
            <div>
                <h4>Ovo je child komponenta, {name}, star je {age} godina</h4>
                <p>Children je: {children}</p>
            </div>
        );
}